# [install] はリポジトリルートの bunfig.toml に一本化した（bun の lockfile はルートに 1 つ）。
# [test] は cwd 基準で解決されるので、このプロダクト直下から `bun test` するときに効く。
[test]
coverage = true
# @qrcc/ui のコンポーネントテストは DOM を要る。bunfig は cwd 基準でしか
# 解決されないため、ルートで一度だけ happy-dom を登録する。
preload = ["./shared/ui/test-setup.ts"]
