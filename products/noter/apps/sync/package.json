{
  "name": "@noter/sync-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "gen": "wrangler types --env-interface CloudflareEnv ./src/worker-configuration.d.ts",
    "typecheck": "bun run gen && tsc -p . --noEmit",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@noter/sync": "workspace:*"
  },
  "devDependencies": {
    "wrangler": "4.127.1"
  }
}
