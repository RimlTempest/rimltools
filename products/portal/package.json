{
  "name": "@rimltools/portal",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run scripts/build.ts",
    "test": "bun test ./src ./scripts",
    "check": "oxfmt --check . && oxlint --type-aware && tsc -p tsconfig.json",
    "dev": "bun run build && wrangler dev",
    "smoke": "bun run scripts/smoke.ts"
  }
}
